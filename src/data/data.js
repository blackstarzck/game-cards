export const getKeys = (table) => {
    const keys = [];
    switch(table){
        case "ALARM_TABLE" :
            keys = [];
        break;
        case "USER_LOG" :
            
        break;
        case "USERS" :
        
        break;
    }
}